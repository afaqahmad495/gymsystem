"use client";

import React from "react";

const Header: React.FC = () => (
  <header className="w-full py-4 bg-linear-to-r bg-white font-sans text-white text-center shadow-md">
    <h1 className="text-3xl md:text-8xl font-extrabold text-blue-500 tracking-tight drop-shadow-lg">
      <span className="text-black">TRAIN HARD.</span> LIVE BETTER
    </h1>
  </header>
);

export default Header;
